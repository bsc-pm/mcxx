# Required for superscalar
set previous ""

if { [ array get env "LD_LIBRARY_PATH" ] != "" } {
    set previous "${env(LD_LIBRARY_PATH)}"
}



set env(LD_LIBRARY_PATH) "../src/tl/superscalar/.libs:${previous}"
